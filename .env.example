# Django — локальна розробка (runserver / docker compose up)
DJANGO_SETTINGS_MODULE=config.settings.develop
SECRET_KEY=dev-only-change-me
ALLOWED_HOSTS=localhost,127.0.0.1,web
CSRF_TRUSTED_ORIGINS=http://localhost,http://127.0.0.1,http://localhost:8000,http://localhost:8080

# Postgres (docker compose)
POSTGRES_DB=didenko
POSTGRES_USER=didenko
POSTGRES_PASSWORD=didenko_dev_pass
DATABASE_URL=postgres://didenko:didenko_dev_pass@127.0.0.1:5432/didenko
HTTP_PORT=8080

# Leads notify
ADMIN_NOTIFY_EMAIL=admin@example.com
DEFAULT_FROM_EMAIL=Didenko <noreply@example.com>
RESEND_API_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# CRM (M5)
CRM_WEBHOOK_URL=
CRM_API_KEY=

# Analytics (IDs only; scripts in template)
GTM_CONTAINER_ID=
GA4_MEASUREMENT_ID=
